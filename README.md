# 🏋️‍♂️ GymPro Secure Bot

Профессиональный фитнес-бот для Telegram с озвучкой упражнений и системой подписки.

## 🛠 Технологии
* **Python** (библиотека pyTelegramBotAPI)
* **SQLite3** для хранения данных пользователей
* **gTTS** для генерации голосовых инструкций
* **python-dotenv** для защиты токена

## 🛡 Безопасность
Все конфиденциальные данные (токен бота и ID администратора) вынесены в файл `.env`, который не попадает в репозиторий. Это защищает проект от кражи ключей.

## 🚀 Запуск
1. Установите зависимости: `pip install -r requirements.txt`
2. Создайте файл `.env` и добавьте туда `BOT_TOKEN` и `ADMIN_ID`.
3. Запустите бота командой `python main.py`.
